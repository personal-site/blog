sudo: false
language: node_js
node_js:
  - v10
  - v8
before_install:
  - gem install bundler
  - bundle install
  - bundle exec jekyll build
install:
  - npm install -g bower gulp
  - bower install
  - npm install
  - npm run build
