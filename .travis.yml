sudo: false
language: node_js
node_js:
  - v8
before_install:
  - npm install -g bower
  - gem install bundler
  - cp _config.yml _config.prod.yml
install:
  - bundle install
  - npm install
  - bower install
  - npm run build
