sudo: false
language: node_js
node_js:
  - v8
before_install:
  - npm install -g bower
  - bower install
  - gem install bundler
  - bundle install
  - bundle exec jekyll build
install:
  - npm install
  - npm run build
