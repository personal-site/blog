sudo: false
language: node_js
node_js:
  - v8
before_install:
  - npm install -g bower
  - bower install
  - gem install bundler
  - bundle install
  - bundle exec jekyll build
  - cp _config.yml _config.prod.yml
install:
  - npm install
  - npm run build
