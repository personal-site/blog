sudo: false
language: node_js
node_js:
  - v8
before_install:
  - cp _config.yml _config.prod.yml
  - sed -i '/analytics_id/a destination: _build' _config.prod.yml
  - npm install -g bower
  - bower install
  - gem install bundler
  - bundle install
  - npm install
install:
  - npm run build
